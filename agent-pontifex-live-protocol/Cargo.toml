[package]
name = "agent-pontifex-live-protocol"
version = "0.1.0"
edition = "2021"
publish = false
description = "Replay-safe, transport-neutral live session contracts for cooperating AI agents"
license = "MIT"
repository = "https://github.com/agent-pontifex/agent-sdk.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
