[package]
name = "agent-pontifex-attestation"
version = "0.1.0"
edition = "2021"
publish = false
description = "Vendor-neutral signed-result artifact transport and trust-routing contracts for Agent Pontifex"
license = "MIT"
repository = "https://github.com/agent-pontifex/agent-sdk.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
